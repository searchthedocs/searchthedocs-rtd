// Production RequireJS configuration.

// Define the paths to modules. No need to define paths or shim config, since
// these are resolved in the build step.
require.config({
    baseUrl: "/static/build/{{ JS_VERSION }}/",
});

